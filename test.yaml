apiVersion: v1
kind: Namespace
metadata:
  name: test

# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: my-app
#   namespace: my-namespace
#   labels:
#     app: my-app
# spec:
#   type: LoadBalancer
#   ports:
#   - name: http
#     port: 80
#     targetPort: http
#     protocol: TCP
#   selector:
#     app: my-app


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world
  namespace: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-world
  template:
    metadata:
      labels:
        app: hello-world
    spec:
      containers:
        - name: app
          #image: brightbox/rails-hello-world
          image: gcr.io/us-con-gcp-npr-0000266-072920/hello-app:v1
          ports:
            - name: web
              containerPort: 3000
              protocol: TCP